This directory contains utilities scripts to make a 2D Calorimeter
Grid and populate the blocks hits per event for ease of visualization
of calorimeter clusters formed.

The make_grid.C code needs an input txt file with the event, total
number of clusters, cluster id, block id, pulse time, and pulse energy
to be read. This txt file will need to be generated by the
replay script via an external parameter